<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_menu"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/colorBack"
    tools:context="com.example.nathan_mccauslin.bloodalcoholcalculator.MenuActivity">

    <LinearLayout
        android:id="@+id/buttonLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:src="@drawable/logo"/>

        <Button
            android:id="@+id/calculateButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="StartCalculatorActivity"
            android:text="Calculate BAL"
            android:textColor="@color/colorEditSecondary"
            android:backgroundTint="@color/colorButtonBack"
            android:textSize="18sp" />

        <Button
            android:id="@+id/impairmentTestButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="StartComingSoon"
            android:text="Impairment Test"
            android:textColor="@color/colorEditSecondary"
            android:backgroundTint="@color/colorButtonBack"
            android:textSize="18sp" />

        <Button
            android:id="@+id/historyButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="StartComingSoon"
            android:text="History"
            android:textColor="@color/colorEditSecondary"
            android:backgroundTint="@color/colorButtonBack"
            android:textSize="18sp" />

        <Button
            android:id="@+id/creditsButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="StartCredits"
            android:text="Credits"
            android:textColor="@color/colorEditSecondary"
            android:backgroundTint="@color/colorButtonBack"
            android:textSize="18sp" />
    </LinearLayout>
</RelativeLayout>
